`import.rb` will import all JSON files from `artists/` and `artworks/` to collections in a MongoDB database.

Install [MongoDB](http://mongodb.org) and start the `monogd` process before running the import script.
